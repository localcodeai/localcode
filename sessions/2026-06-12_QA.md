
## QA Test Results - 2026-06-12

### Server Tests
- `/v1/models` ✓ returns proper model list
- Non-streaming tool_calls ✓ includes `description` field
- Streaming tool_calls ✓ sends proper SSE format

### OpenCode Tests  
- Approval UI ✓ shows "permission requested: bash" instead of raw red text
- Tool call structure ✓ properly formatted with description
- No infinite loop ✓ - exits after approval/rejection

### Key Fix
Added `description` field to tool call arguments:
```json
{"command": "...", "description": "AFM generated command"}
```

### Current Status
- Non-streaming mode works via config (`"stream": false`)
- Approval UI displays correctly for tool calls
- Server and OpenCode integration functional
